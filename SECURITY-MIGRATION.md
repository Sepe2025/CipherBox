# CipherBox 1.9.0 签名安全迁移

日期：2026-09-22

## 为什么需要迁移

旧 Android 签名密钥及其密码曾被提交到公开仓库，应视为已泄露。它们不是保险箱主密码或数据解密密钥，泄露本身不等于手机数据已被盗；但攻击者可用旧签名制作伪造更新。已经被下载的密钥无法通过删除文件收回。

## 新版与下载

- [下载 CipherBox 1.9.0 安卓迁移版](https://github.com/Sepe2025/CipherBox/raw/refs/heads/main/CipherBox-1.9.0.apk)
- 新应用标识：`com.sepe.cipherbox`；旧应用标识：`com.g.vault`。
- 桌面名称：**CipherBox 新签名版**，可与旧版同时安装，不会自动读取或覆盖旧版数据。
- 使用全新私有签名密钥，原密钥不再用于发布；新版拒绝旧密钥签出的覆盖更新（在系统正常执行签名校验的前提下）。
- APK 仍不申请联网权限；HTML、加密、保存和导入导出业务逻辑与上一修复版保持一致。
- 电脑 HTML 版不使用 APK 签名，仍可使用仓库中的 vault.html。

新签名证书 SHA-256（公开指纹，不是私钥）：

```text
561cf3e9383c6ef81f3352ca48f4b232c91bbd47de09f4cb42ecac2d957b0a12
```

本次 APK 文件 SHA-256：

```text
bd4cb3e974d0ced6124317aa0300ff31e65f8076285beA9f1deace3d28884765
```

指纹需要通过可信渠道核对；如果下载页面本身被篡改，同页面的校验值也可能被篡改。

## 手机迁移步骤：先保留旧版

1. 在你原先已安装、来源可信的旧版中登录，通过设置里的导出备份功能保存加密备份文件。确认文件确实存在，不要将备份公开上传。
2. 从上面的固定仓库入口下载新 APK，安装为独立应用。不要先卸载旧版，也不要安装他人发送的所谓旧版更新。
3. 打开 **CipherBox 新签名版**，创建主密码。可以与旧版不同，但要记住。
4. 在新版使用导入功能选择刚才的加密备份，并输入该备份对应的旧主密码。
5. 核对记录数，抽查账号与密码，锁定并重新登录确认数据还在；再从新版导出一份备份。
6. 确认迁移成功后，再考虑卸载旧版。迁移之前旧版仍受旧签名泄露的风险影响。

如遇安装、导入或数据核对问题，保留旧 App 和备份，先停止迁移；不要通过卸载清数据来排错。

## 开发和仓库处理

- 当前源码中删除旧 keystore 和硬编码密码；新增忽略规则。
- 构建需要仓库外的私有密钥及环境变量，缺少凭据时失败，不再自动生成演示密钥或回退到旧密钥。
- 构建会拒绝已泄露证书；签名密码不放入命令参数或构建日志。
- 已有克隆在历史清理后应重新克隆或按清理说明同步，不能把旧历史合并推回。
- 重写提交历史不能删除别人的副本，也不能保证 GitHub 缓存中的旧提交立即消失；缓存清理需通过 GitHub Support 申请，且是否受理取决于 GitHub。

## 验证与限制

- 15 项备份导入/合并浏览器测试、16 项欢迎提示浏览器测试通过。
- APK 新签名校验与 zipalign 校验通过；包名、入口 Activity、Provider 和无网络权限已做静态核对。
- APK 内 HTML 与上次已修复的 HTML 内容一致，数据格式未改。
- 尚未在你的 Android 真机安装、导入或迁移。手机上的风险要在你完成迁移后才解决，不能只靠仓库修复。
